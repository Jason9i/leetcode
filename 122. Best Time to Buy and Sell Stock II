class Solution:
    def maxProfit(self, prices: List[int]) -> int:
        result = 0
        len_p = len(prices)
        start = prices[0]
        i=1

        while i < len_p:
            if start <prices[i]:
                result += prices[i]-start
            start = prices[i]
            i+=1
    
        return result    
