class Solution:
    def isPalidrome(self, x: int) -> bool:
        x = str(x)
        len_x = len(x)
        l, r = 0, len_x -1

        while l<=r:
            if x[l] == x[r]:
                l+=1
                r-=1
            else:  
                return False
        return True