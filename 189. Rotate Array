class Solution:
    def rotate(self, nums: List[int], k: int) -> None:
        """
        Do not return anything, modify nums in-place instead.
        """
        len_n = len(nums)
        if k % len_n == 0:
            return nums

        while k:
            last = nums[len_n-1]
            nums.pop()
            nums.insert(0, last)
            k-=1
        return nums       